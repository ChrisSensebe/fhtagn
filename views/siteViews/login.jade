extends siteLayout

block content
    .container
        .row
            .col-md-12
                h2.page-header #{pageContent.pageTitle}
                form(method="post" action="/login")
                    label(for="username") #{pageContent.labelForUSername}
                    input(class="form-control" id="username" name="username" required)
                    label(for="password") #{pageContent.labelForPassword}
                    input(class="form-control" id="password" name="password" type="password" required)
                    button.btn.btn-primary.btn-form #{pageContent.loginButtonText}