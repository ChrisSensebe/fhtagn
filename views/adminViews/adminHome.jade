extends adminLayout

block content
    .container
        .row
            .col-md-12
                h2.page-header #{page.pageTitle}
        each post in page.posts
            .row
                .col-md-12
                    h3= post.title
                    p #{page.dateCreatedText + post.created}
                    p #{page.dateUpdatedText + post.updated}
                    p #{page.authorText + post.author}
                    p #{page.tagsText + post.tags}
                    p
                        a(href="/admin/post/#{post.id}" class="btn btn-primary") #{page.editPostButtonText}
                    p
                        form(method="post" action="/admin/delPost")
                            input(type="hidden" id="postId" name="postId" value="#{post.id}")
                            button.btn.btn-danger #{page.deletePostButtonText}
        .row
            .col-md-12
                p
                    a(href="admin/newPost" class="btn btn-default") #{page.newPostButtonText}
